name: Just test caching in Github Action
on: [push]
jobs:
  Lint:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v2
      - uses: action/setup-node@v2
        with:
          node-version: '14'
      - run: npm run lint
